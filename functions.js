const numeros3 = [3, 8, 9, 15, 12, 29];
const novoArray = [0],llll

const reordenandoArray = (array, num) => {
  this.array.forEach(i => {
    if (num === i) {
      const numeroEscolhido = num;
      numeros3.splice(this.numeros3.indexOf(num), 1);
      console.log(this.numeros);
      novoArray = [num, ...numeros3];
    }
  })
}